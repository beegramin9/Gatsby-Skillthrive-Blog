import React from 'react';
import {CardArrayWrapper} from '../styles';

export const CardArray = ({children}) => {
    return (
        <CardArrayWrapper>
            {children}
        </CardArrayWrapper>
    )
}

